package lwjgl.engine.common.input

enum class FunctionKey {
    ESCAPE,
    ENTER,
    SPACE,
}

enum class PrintableKey {
    Q, W, E, R, T, Y, U, I, O, P,
    A, S, D, F, G, H, J, K, L,
    Z, X, C, V, B, N, M
}
